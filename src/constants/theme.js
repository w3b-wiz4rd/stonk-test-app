import colors from './colors';

const theme = {
  background: colors.darkBlue,
  navigation: {
    background: colors.darkBlue,
    tabIconActive: colors.white,
    tabIconInactive: colors.gray,
  },
  typography: {
    color: {
      main: colors.white,
    },
  },
};

export default theme;
